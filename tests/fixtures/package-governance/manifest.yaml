schema_version: 1
defects:
  - {id: manifest-license-drift, test: test_rejects_manifest_metadata_drift, expected_diagnostic: "license must equal 'MIT'"}
  - {id: competing-version-authority, test: test_rejects_marketplace_authority_drift, expected_diagnostic: must not duplicate the manifest version authority}
  - {id: installed-license-drift, test: test_rejects_license_carrier_drift, expected_diagnostic: license must match repository license exactly}
  - {id: orphaned-skill-lifecycle, test: test_rejects_orphaned_skill_lifecycle, expected_diagnostic: owner is required}
  - {id: retirement-without-migration, test: test_rejects_retired_skill_without_migration, expected_diagnostic: non-active skill requires migration treatment}
  - {id: incomplete-consumer-enumeration, test: test_rejects_incomplete_consumer_enumeration, expected_diagnostic: must contain claude-ai and claude-code exactly}
  - {id: portable-core-version-drift, test: test_rejects_portable_core_version_drift_from_accepted_adr, expected_diagnostic: portable_core must match the accepted ADR-001 version}
  - {id: portable-core-provenance-drift, test: test_rejects_portable_core_authority_provenance_drift, expected_diagnostic: ADR-001 source must identify the accepted portable-core authority}
  - {id: compatibility-portable-core-drift, test: test_rejects_compatibility_portable_core_version_drift, expected_diagnostic: portable-core identity must match the registry}
  - {id: claude-code-minimum-semantics-drift, test: test_rejects_claude_code_minimum_version_semantics_drift, expected_diagnostic: Claude Code capability must declare a minimum-supported SemVer floor}
  - {id: compatibility-minimum-host-drift, test: test_rejects_compatibility_minimum_host_drift, expected_diagnostic: minimum supported Claude Code version must match the registry}
  - {id: verified-host-below-minimum, test: test_rejects_verified_host_below_minimum, expected_diagnostic: verified Claude Code host is below the supported minimum}
  - {id: nonexact-verified-host, test: test_rejects_nonexact_verified_host_identity, expected_diagnostic: verified Claude Code hosts must use exact SemVer identities}
  - {id: ci-host-floor-drift, test: test_rejects_ci_host_floor_drift, expected_diagnostic: Claude Code CI pins must exercise the supported minimum}
  - {id: vocabulary-schema-drift, test: test_rejects_vocabulary_schema_drift, expected_diagnostic: gate_status must match the governing schema enum exactly}
  - {id: uncontrolled-duplicate-authority, test: test_rejects_uncontrolled_duplicate_authority, expected_diagnostic: every repetition requires an allowed classification and drift control}
  - {id: stale-context-measurement, test: test_rejects_stale_context_measurement, expected_diagnostic: catalog description measurement is stale}
  - {id: missing-package-finding, test: test_rejects_missing_package_finding, expected_diagnostic: must contain PKG-001 through PKG-039 exactly once}
  - {id: missing-governance-link, test: test_rejects_missing_governance_link, expected_diagnostic: package-governance link target does not exist}
  - {id: missing-package-identity-carrier, test: test_rejects_missing_package_identity_carrier, expected_diagnostic: package identity carrier is missing}
  - {id: malformed-package-identity-carrier, test: test_rejects_malformed_package_identity_carrier, expected_diagnostic: package identity carrier is not valid JSON}
  - {id: stale-package-identity-version, test: test_rejects_stale_package_identity_version, expected_diagnostic: package identity carrier does not match the manifest authority}
  - {id: stale-package-identity-digest, test: test_rejects_stale_package_identity_digest, expected_diagnostic: package identity carrier does not match the manifest authority}
  - {id: divergent-package-identity-copy, test: test_rejects_divergent_package_identity_copy, expected_diagnostic: package identity carriers must be byte-identical}
  - {id: package-identity-contract-drift, test: test_rejects_package_identity_contract_drift, expected_diagnostic: package identity carrier does not match the manifest authority}
  - {id: package-identity-extra-field, test: test_rejects_package_identity_extra_field, expected_diagnostic: package identity carrier does not match the manifest authority}
  - {id: nested-extra-package-identity-carrier, test: test_rejects_nested_noncanonical_package_identity_carrier, expected_diagnostic: package identity carrier is unexpected}
  - {id: cli-extra-package-identity-carrier, test: test_cli_entrypoints_report_same_unexpected_package_identity_carrier, expected_diagnostic: package identity carrier is unexpected}
  - {id: stale-directory-package-identity-carriers, test: test_rejects_every_package_identity_carrier_in_stale_skill_directories, expected_diagnostic: package identity carrier is unexpected}
  - {id: write-with-extra-package-identity-carrier, test: test_write_refuses_unexpected_package_identity_carrier_before_mutation, expected_diagnostic: package identity carrier is unexpected}
  - {id: release-before-acceptance, test: test_release_mode_fails_closed_before_acceptance, expected_diagnostic: release mode requires --release-evidence}
