# Fulfillment-recovery physical runtime binding

| Field | Value |
|---|---|
| Parent construct | [FRD-EXECUTION-CONTRACT](execution-contract.md) |
| Revision | draft-1 |
| Status | PROPOSED; PB-runtime, amended PB-transport, PostgreSQL engine, database credential condition and all five EN designs accepted; exact instance enrollment and remaining bindings open; not an implementation-complete contract or execution authorization |
| Owner | Tad Haffey |
| Recorded | 2026-09-11 (America/New_York) |
| Review trigger | Before accepting a runtime choice, producing launchable configuration, or freezing wire/event fixtures |
| Doctype ruling | Companion binding for the existing internal proving fixture; not a new standing standard or HEX architecture |
| Deliberation substrate | Four accepted EC decisions and EXEC-SEQUENCE in the [ratification record](agent-code-v2-ratification-record.md); operator's subsequent instruction to proceed with physical-contract drafting |
| Supersedes | None; accepted behavior and public scenario bytes remain unchanged |

## 1. Purpose, read depth and evidence floor

Recommend concrete runtime, transport and persistence choices against the accepted boundaries. A supplied fixture must provide real services without supplying the agents' solution. Its topology must also permit actual process death, retained effects and independent inspection.

Read whole for this pass: repository orientation, parent execution contract, Bedrock construct-spec contract and its template/evidence reference, Bedrock testing contract and its Haffey profile/test-strategy schema. Fresh scoped authority reads: proving profile §7, consumer design §§1–2, and the final EC ratification. The public case registry was not re-read field by field; the retained authoring checker re-inventories it. No hidden population was inspected or created.

Native Mac read-only probes found Docker CLI/server 29.7.2, Docker Desktop 4.89.0, Linux arm64 server, and Compose v5.5.0. The first server query was denied by the execution sandbox; the same read-only query succeeded with approved access. This is one host's capability observation, not fixture startup, security testing, image availability, reproducibility or campaign freeze. No container was started, pulled, built, stopped or modified. Exact command results are retained in [authoring evidence](scenarios/proposed/physical-runtime-authoring-checks.json).

Vendor-document read depth: Docker network and engine-security articles whole; selected Compose service sections for capabilities, read-only roots, restart, secrets and security options; SQLite transaction, isolation and backup articles whole, and the relevant synchronous-setting discussion. These document capabilities, not this fixture's enforcement. Recall episodes about inherited review history and ambient pytest configuration were read as leads; their historical implementation claims were not re-proved or imported as authority. The current proof contract independently requires a fresh, explicitly supplied builder context.

The [PB-runtime-ratification authoring output](scenarios/proposed/pb-runtime-ratification-authoring-checks.json) preserves the subsequent document-integrity and static-reference rerun. It does not refresh the earlier Docker observation into a new runtime result. That pass proposed TLS after reading the OWASP HTTPS section in full, not the entire cheat sheet. The operator subsequently accepted PB-transport with HTTP instead for this local proving run, avoiding certificate overhead; the earlier TLS proposal is superseded for the fixture interfaces, not provider egress.

For this amendment, the physical binding, repository orientation, construct-spec skill/template/evidence guidance and README were read whole. Parent-contract decision-status/next-gate passages, scenario-guide execution bindings and the latest ratification sections were read as scoped passages, not a fresh whole-corpus review. The [HTTP-amendment authoring output](scenarios/proposed/pb-transport-http-authoring-checks.json) retains the new integrity and static-reference checks; it is not network-isolation or execution proof.

The subsequent PB-persistence ratification selects PostgreSQL, replacing the unaccepted SQLite proposal. This pass read the physical binding, README and construct-spec instructions whole, and the parent/scenario-guide status and affected persistence passages plus the latest ratification as scoped reads. It read the identified PostgreSQL container's complete mounted authentication/server configuration, selected effective server settings and the complete parsed authentication-rule rows; it did not read business data, credentials, all server settings or a role/database inventory. PostgreSQL 17 transaction-isolation and authentication-view documentation were read whole; HBA authentication options and snapshot-export guidance were scoped reads. Actual commands, outputs and authoring checks are retained in [PB-persistence evidence](scenarios/proposed/pb-persistence-postgresql-authoring-checks.json). These observations do not enroll the server or prove fixture isolation.

The subsequent database-credential ratification accepts preserving that security requirement for the reuse path; it does not complete enrollment. For this codification, repository orientation, construct-spec instructions/template/evidence guidance, the recall project sheet and README were read whole; affected physical-binding, parent/scenario-guide status and ratification passages were scoped reads. A truncated combined read was disclosed and the affected passages reread in smaller sections. The [credential-ratification authoring report](scenarios/proposed/pb-persistence-credentials-authoring-checks.json) retains fresh document checks. No new Docker, SQL or authentication probe was performed; the preceding inspection remains historical evidence with its stated limits.

## 2. Decisions for individual ratification

PB-runtime's trust-boundary model and transport-only relay were ratified by Tad Haffey on 2026-09-11 (America/New_York). PB-transport was accepted the same day with the operator's sole amendment: HTTP rather than HTTPS for this proving run. PB-persistence's PostgreSQL engine choice and subsequently its database-only certificate/credential condition were ratified, with enrollment of the existing instance explicitly conditional on the remaining access/lifecycle checks; see the [ratification record](agent-code-v2-ratification-record.md). Acceptance does not settle every subsequent implementation detail.

| Decision | Recommendation | Alternative and trade-off |
|---|---|---|
| PB-runtime | Accepted model: local Docker Compose runtime with separate application, fixture and offline-evaluator trust boundaries; a transport-only provider egress relay; host custodian retains lifecycle control | Same-user native processes are lighter but do not enforce the required filesystem boundary. A separate VM/host remains an option if the declared container threat model or actual access tests are insufficient; it adds setup and evidence-custody work. |
| PB-transport | Accepted as amended: direct, versioned plain HTTP/JSON for the local fixture's public actions and observation/hooks, scoped opaque credentials, and a separate private administration channel | HTTPS would add certificate setup that the operator declined for this isolated synthetic proving run. MCP adds a protocol/lifecycle surface without a current need for discovery; stdio complicates concurrent clients and independent service lifetime. This choice does not prescribe transport for future HEX applications. |
| PB-persistence | Accepted engine: PostgreSQL behind the fixture; preserve separate application-owned durable state and immutable evaluator evidence. Database-only TLS with restricted fixture login, password and matching client certificate accepted. EN-scope accepts dedicated HEB-154 run databases; exact credential/database bindings and instance enrollment remain pending | SQLite was originally recommended for low setup cost, not a capability PostgreSQL lacked. Verified local PostgreSQL availability changed that trade-off. Existing-instance reuse must still respect its authentication, shared lifecycle and data boundaries; a separate PostgreSQL instance would require a separate hosting disposition. |

All three PB decisions and the five EN design directions are accepted within their recorded scopes; PostgreSQL server enrollment is not. Exact access/lifecycle bindings, their implementation and verification, and the remaining physical contract are still pending. There is no acceptance of a product framework, provider/model, candidate implementation, live budget, evaluator threshold or content-retention policy in these decisions.

## 3. PB-runtime — topology and access

The trust-boundary model and transport-only provider relay are accepted. PB-transport also accepts the separate private administration socket model below. Exact network names, socket ownership/commands, launch settings and other physical bindings remain proposed or pending; runtime access enforcement and the separate builder-isolation gate are unproved.

### Trust and supplied components

The trusted computing base is the operator/custodian, host OS, container engine, supplied fixture, its PostgreSQL service/administrators and evidence collector. The boundary protects hidden material and service state from the cold builder and built application. It is not a claim of protection against a hostile host/database administrator or an unknown container escape. Such a threat would reopen the runtime choice.

Docker's documented namespace and capability mechanisms support the proposal, but its security documentation also identifies daemon access and overly broad mounts as risks. Consequently, enforcement must be inspected and exercised rather than inferred from a container name. [Docker Engine security](https://docs.docker.com/engine/security/)

| Runtime actor | Supplied access and durable state | Excluded access |
|---|---|---|
| Host supervisor/custodian | Exact run/container identities, lifecycle operations, private fixture administration, curated build-input export and evidence custody | Must not use its authority to coach an answer into a run or silently repair a failed first result |
| `desk` application container | Builder-authored application image; its own durable checkpoint/artifact volume; authorized business-action credentials and approved provider credentials | Host home/repository/recall, authoring history, Docker socket/API, fixture volume, hidden schedules, expected labels and evaluator storage |
| `fixture` container | Authoritative business services, scoped PostgreSQL access for state/journal/grants/scheduling, and trusted service-event collection | No need for provider credentials, natural-language plan generation, product recovery orchestration or evaluator answer labels |
| PostgreSQL service (instance enrollment pending) | Durable fixture-owned run state and history, accessible to the fixture and authorized custodian/export processes | No direct product-agent SQL access; no fixture credentials in model text; no unrelated workbench data in HEB-154 evidence |
| `provider-egress` relay | Frozen allowlist of provider destinations; transport diagnostics limited by content policy | No fixture administration, world/evaluator volumes, request rewriting, provider adaptation, prompt construction or automatic application-request replay |
| Offline evaluator container | Read-only, immutable evidence export plus separately held expectations; separate writable result directory | No mutation path to the first run, no application/fixture credentials, no default network access |

The fixture may contain the business service and its private scheduler/collector as separately identified processes within the same trusted container. They are not independent judges. The offline evaluator checks persistent state and authoritative inputs without importing the service's business-validation implementation as its oracle. Any later model-based semantic evaluation requires its own approved provider/custody binding; the offline deterministic evaluator does not authorize that access.

### Network and filesystem realization

Propose two explicit internal networks: `business` connects only desk and fixture; `provider-link` connects only desk and egress relay. The relay alone also joins an external-egress network. No fixture, desk or admin port is published on the host. No application container joins host networking, receives extra host mounts, or obtains the engine API. Compose supports explicit network membership and externally isolated internal networks; those features alone do not prove the whole allowed/denied path matrix. [Compose networks](https://docs.docker.com/reference/compose-file/networks/)

PostgreSQL selection adds a fixture-to-database connection whose exact network bindings remain pending enrollment. The desk must not gain a database route or credentials by joining a workbench network or by opening general host-gateway access. The existing server's loopback-published SQL port is an observation, not an accepted new public fixture endpoint or a proven container-to-server path. EN-route in §5 accepts a separate internal database bridge as design, not the workbench network or host-gateway route. Bind and verify the route before use; network attachment and shared-server changes still require separate authorization.

Provider egress uses an allowlisted TLS tunnel to selected provider hosts and ports. TLS remains end-to-end between the builder's adapter and provider. The relay must reject unapproved targets, private/loopback/link-local destinations and redirection or name-resolution routes around the allowlist. It does not terminate provider TLS or collect model content. Actual DNS, host-gateway, IPv4/IPv6 and direct-egress denial require controls before live evaluation. Relay software, exact policy, destinations and credential delivery remain freeze bindings, not existing infrastructure.

Use the accepted private administration model: a Unix-domain socket inside the fixture's private filesystem. The host supervisor reaches a fixed administrative client through the engine's exec channel, with an exact container ID and structured input, not a caller-supplied shell command. The public listener exposes no admin route. The desk cannot mount that socket or invoke the engine. This avoids treating a second port on the same reachable listener as an access boundary. Exact administrative commands, socket ownership and hook messages remain to be frozen.

Use non-root users, a read-only application root, explicitly writable per-run volumes/tmpfs, dropped Linux capabilities, `no-new-privileges` and no automatic container restart. Service process death is not repaired by an implicit Compose policy. Pin images by immutable identity and record the effective launch configuration; freeze resource limits with campaign budgets. Inspect the realized mounts and credential readability: declaring a Compose secret does not by itself establish its desired file ownership. [Compose service settings](https://docs.docker.com/reference/compose-file/services/)

Under accepted RUN-manifest-identity, the supervisor supplies and every launch consumer verifies the same external `{run_id, manifest_digest, manifest_artifact_sha256}` triple. The semantic digest uses domain-separated JCS over the complete closed manifest; the artifact digest uses its exact UTF-8 file bytes. Neither value authenticates the supervisor or replaces the separate credential/access boundary. Exact delivery mechanics and runtime checks remain pending physical bindings.

RUN-manifest-boundary keeps the complete private manifest with the supervisor and eventual evaluator rather than mounting it into every container. The fixture receives only its required configuration, private schedules, authorization, sources and limits. The application receives a separate non-coaching projection with no hidden schedule, private policy configuration, evaluator material or credentials. Runtime logs/evidence link to the immutable identity triple and never rewrite the manifest. Exact projection files, mounts and validation remain pending.

RUN-consumer-projections requires those fixture/application documents to be closed deterministic derivatives carrying the parent identity triple and exact audience. The supervisor retains their raw artifact hashes in launch evidence without adding semantic projection digests or a projection-hash cycle in the parent manifest. Each consumer validates audience, parent identity and exact derivation before admission; the evaluator later regenerates and byte-compares both. Exact payload fields and delivery remain pending.

RUN-manifest-root fixes the private configuration root at twelve fields covering revision/run, campaign, contracts, accepted sources/authorization, candidate versus built application, fixture, providers, execution and evidence policy. It adds no root timestamps, mutable status/results, secrets or digest self-reference. Runtime topology and launch design must consume the later closed nested records without adding an unreviewed thirteenth root field.

RUN-campaign-binding closes the campaign record at five fields: campaign/revision/configuration IDs, public-or-hidden population and positive trial ordinal. A separate campaign ledger enforces ordinal uniqueness within the campaign/revision/configuration/selected-case scope. Case/world stay in source selection, and provider/application/execution settings stay in their own records; their later configuration IDs must agree. The launch path must not add timestamps, seeds, outcomes, verdicts or a redundant campaign digest to this block.

RUN-contracts-binding closes the manifest's contract reference at three fields: contract-set ID, revision and exact `{path, sha256}` artifact. The separately closed contract-set artifact carries the complete deterministic inventory of governing contracts and executable schemas. Before launch admission, the supervisor verifies its bytes and every named member; an outer hash alone is insufficient. The manifest does not duplicate the list or add a contracts digest, and this decision does not promote the current candidate bindings. The fixed member roles/schema are accepted below; exact delivery remains pending.

RUN-contract-set-membership fixes that artifact to revision `frd/contract-set/v1` and twelve required `{path, sha256}` roles spanning ratification, the standard/profile/consumer/business authorities, scenario/execution/wire/runtime/persistence contracts, the verification contract and schema pack. Emit JCS bytes and fail launch admission on a missing/unknown role or missing/changed member. SCHEMA-pack-membership fixes the content-addressed manifest mechanics, offline project-local reference closure and current 42-JSON-schema v2 seed solely for HEB-154 testing of the `agent-code` skill. VERIFY-contract-shape fixes the verification plan's exact candidate/authority/strategy/population/environment/suite/policy bindings and G1–G8 all-pass rule without results. VERIFY-deterministic-thresholds fixes 100% registered fixture and dirty-control passage, 90% line and branch coverage, 80% mutation score and 100% critical branch/state exercise. VERIFY-evaluation-sampling and VERIFY-evaluation-thresholds fix the draw design, provider-configuration separation, confidence floors, per-case rule, zero-tolerance invariants and evaluator-control gate. VERIFY-model-settings fixes candidate product-role model/request settings. Final executable DDL, grants, hook/admin/evidence schemas, export encodings, the verification strategy and real artifacts, hidden members, eligible denominators, physical provider profiles, evaluator/Gemini settings, frozen pricing, spend authorization and actual launch artifacts remain pending. These decisions make no Agents of HEX overall-design choice.

RUN-candidate-binding closes the run's candidate record at candidate ID/revision and one exact `{path, sha256}` build-input-manifest artifact. The referenced manifest owns the complete curated cold-builder export and allowed starting inputs; the supervisor verifies it and every member before construction. Built output and build-session evidence stay out of this pre-execution reference, as do hidden evaluation material, provider settings, budgets and credentials. Exact build-input-manifest membership is fixed below; population, builder-runner enforcement and actual cold construction remain pending.

RUN-build-input-membership fixes the curated export inventory at ten categories and requires parent candidate equality. All artifact arrays except starting components are nonempty; a required empty starting-components array explicitly proves no supplied component. Arrays are duplicate-free and ASCII path/hash ordered, and the manifest is emitted as JCS bytes. The supervisor verifies every artifact before delivery. Hidden/evaluator material, secrets, provider/execution settings and build-use evidence are excluded; runner isolation, delivery/read receipts and actual construction remain separate proof gates.

RUN-application-binding closes the run's produced-application record at application ID/revision, campaign-matching configuration ID and one exact `{path, sha256}` application-manifest artifact. That referenced manifest owns the resulting implementation, immutable image and dependency identities; the supervisor verifies it and every member before admission. Build receipts/logs and builder/read/use/intervention/result evidence remain external, as do provider settings, budgets, credentials and fixture configuration. Exact application-manifest membership is fixed below; actual construction, launch configuration and runtime proof remain pending.

RUN-application-manifest-membership fixes the retained build output as one exact implementation bundle, one loadable OCI-image artifact with its immutable manifest digest and one realized-dependency inventory, all bound to the parent application ID/revision and emitted as JCS bytes. The supervisor verifies all artifact bytes and the image digest observed after load. Experimental configuration stays in the outer run record; build provenance/evidence and launch records stay external. Real artifact generation, dependency-inventory format, image loading and launch proof remain pending.

RUN-fixture-binding closes the run's fixture record at fixture ID/revision and one exact `{path, sha256}` fixture-manifest artifact. The referenced manifest owns fixture implementation/image, persistence, controlled clock, service limits and schedule/fault configuration; the supervisor verifies it and every member before admission. Authorization/source selection remain separate fixture-projection inputs. Credentials, database enrollment, runtime state/evidence/exports/results stay external, and no campaign configuration ID is added. Exact fixture-manifest membership is fixed below; initialization and runtime proof remain pending.

RUN-fixture-manifest-membership fixes the retained fixture definition as one implementation bundle, one loadable OCI-image artifact/digest, one persistence definition and exact clock/limits/schedule/fault-plan artifacts bound to the parent fixture and emitted as JCS bytes. Schedule and fault-plan artifacts remain required when empty. The supervisor verifies all seven artifacts, the image digest observed after load and deterministic schedule/fault derivation. Inner configuration schemas, real artifact generation, enrollment, initialization and launch proof remain pending.

FIXTURE-config-shape fixes the four inner boundaries. Controlled business time is selected-world-derived, declared-event-only and independent of real monotonic deadlines. Service limits bind one active run, two concurrent external operations, four fixture SQL connections/no overflow/no checked-out connection during waits and the accepted wire ceilings. The private schedule carries every selected-case event once in source order. The fault plan references those event IDs and adds versioned mechanism/exposure facts without local time/order; empty remains explicit. Admission re-derives and byte-compares. Real mechanism contracts/artifacts and runtime proof remain pending.

FIXTURE-event-binding derives each event ID as `<case-id>:event-<NNN>` from its one-based source ordinal. Every accepted kind remains schedule-valid; the fault plan contains all and only the 15 ratified boundary-intervention kinds in schedule order, selecting 21 of 64 current public event occurrences. Other events remain ordinary scheduled state/policy/source/concurrency/control. Real mechanism contracts and runtime proof remain pending.

FIXTURE-sql-deadlines fixes monotonic pool-acquisition/connection/lock/statement/total-transaction limits at `3/3/2/5/10` seconds. Lock expires before statement, transaction remains inside the 15-second fixture-operation ceiling, and five seconds remain for cancellation and outcome classification. Case controls only tighten. These are proving guardrails with zero runtime calibration; exact enforcement and runtime proof remain pending.

FIXTURE-mechanism-artifacts fixes one immutable case/event-bound JCS contract per intervention occurrence and derives its ID as `<schedule-event-id>:mechanism`. The contract owns injection behavior, effect/application disposition, evidence requirements and exact compatibility; the schedule alone owns time/order. Five provider-native kinds require exact profile/adapter bindings; the remaining ten require exact fixture/harness bindings. Candidate controls cover 21 unique public occurrences. Real contracts, implementations/profiles and exposure proof remain pending.

RUN-providers-binding fixes one three-role provider matrix per campaign configuration: investigator, planner and correspondent each bind profile ID/revision/artifact, requested model and exact settings artifact. Configuration ID agrees with campaign/application/execution. The supervisor verifies artifacts, profile identities and capability sufficiency before admission. Provider credentials and account/project/region enrollment travel separately; requested-model configuration remains distinct from returned-model/usage/attempt evidence. Actual profiles, settings, connectivity, spend authorization and calls remain pending.

RUN-execution-binding fixes the run's execution record at execution ID/revision, configuration ID agreeing with campaign/application/providers, and one exact execution-manifest artifact. That manifest owns required capabilities, joint budgets, concurrency, deadlines, retry ceilings, cancellation/reconciliation controls and operational limits; its exact membership and policy values are fixed below. The supervisor verifies the artifact bytes before admission. Provider profiles own retry defaults and disable/count mechanisms, while execution owns permitted ceilings. Credentials/enrollment/spend authorization and runtime attempts/usage/state/evidence/results travel separately.

RUN-execution-manifest-membership fixes that artifact at execution ID/revision and exact capability-requirements, aggregate-budget and control-policy artifacts. The supervisor verifies all three member bytes and parent identity before admission. The artifacts separate provider-sufficiency requirements, joint resource/pricing ceilings and procedural continuation/stop controls. All three inner policies and candidate schemas are authored; actual spend authority and runtime proof remain pending.

RUN-evidence-policy-binding fixes the final root record at evidence-policy ID/revision and one exact manifest artifact, common across a campaign revision. That manifest owns content capture/classification/redaction, custody/access/storage/encryption/retention/deletion and required-evidence/reviewability configuration. Stricter provider constraints prevail. The supervisor verifies its bytes; produced evidence, runtime state, results/verdicts and secrets remain outside. All three inner policies are accepted; real artifacts and physical enforcement/custody proof remain pending.

RUN-evidence-policy-manifest-membership fixes that artifact at policy ID/revision and exact content-policy, custody-policy and evidence-requirements artifacts. The supervisor verifies parent identity and all three member bytes. Capture stays off until the accepted content/custody policies are physically bound, implemented, tested and frozen and launch is separately authorized; keys/secrets and produced evidence remain outside. Real artifacts and actual enforcement/custody proof remain pending.

EVIDENCE-content-policy permits exact fabricated non-sensitive proving content only in the governed collector, while excluding provider reasoning, credentials/authorization material, unrelated host content and non-synthetic external content. Application logs and telemetry remain structural-only; the relay remains transport-only. Prohibited post-receipt content stops capture and makes semantic review unavailable. The supervisor may not activate capture before custody controls pass, freeze and separate launch authorization.

EVIDENCE-custody-policy requires private collection outside builder/application access and immutable per-run content-addressed encrypted packages under the canonical evidence root. Keys remain separate. Evaluator/reviewer access is read-only only after builder isolation and sealing; builder, application, fixture and relay cannot read content storage. Keep a primary and verified encrypted replica while the claim is current. No automatic expiry or teardown deletion; explicit operator authorization, exact-run targeting and a receipt are required. Exact paths, identities, encryption implementation and custody-control proof remain pending physical bindings, and no capture is activated.

EVIDENCE-requirements fixes the required authority, deterministic, live-provider, consumer and review/acceptance groups; complete frozen-ledger accounting; three-view correlation; artifact identity; fail-closed partial/unavailable handling; dirty/oracle controls; first-result lineage; and cold-review constraints. Exact populations, thresholds, providers and storage mechanisms remain in their owning freeze artifacts. No evidence record or result is instantiated here.

EXEC-capability-requirements fixes the three role output/tool/streaming requirements and common provider-profile admission semantics. Investigator/Planner tools remain serial, client-managed and controller-mediated; Correspondent has none. Provider-managed business effects and parallel calls are disabled, and unknown/unsupported capability blocks before transport. Candidate models/request settings are fixed by VERIFY-model-settings; physical profiles, SDK/dependency versions and capability checks remain pending, and this creates no adapter or provider access.

EXEC-aggregate-budget fixes one restart-persistent per-run budget across all roles/orders/recovery: eight physical model attempts, 24 controller tool operations, 1,200 monotonic seconds, 80,000 input tokens, 16,000 output tokens, two concurrent external operations and 5,000,000 USD micros. All physical retries/reconnects/recovery/fallback and reconciliation/replay count. Complete pre-dispatch fit is required; equality exhausts, unknown usage/pricing blocks and case controls only tighten. Physical pricing and metering proof remain pending; no spend is authorized.

EXEC-control-policy fixes monotonic deadlines at 90 seconds per model attempt; 15 seconds per fixture operation and reconciliation; five seconds per checkpoint and cancellation grace; and 30 seconds for hook/barrier and restart readiness. Provider/SDK retries, reconnects and fallback are zero. One same-identity ordinary-failure model recovery, one proven-no-effect read retry, two reconciliations, one permit-bound replay and one scenario-scheduled restart are the only continuation allowances; mutation redispatch is forbidden. Cancellation blocks/propagates immediately, preserves confirmed effects and stops after the grace. Identity, evidence/custody/content, budget, required-service and application/controller/persistence integrity failures abort before another dispatch; first results/evidence/known effects persist and remediation requires a new run identity. Case controls only tighten. Runtime proof remains pending; no execution is authorized.

Each campaign run gets fresh fixture and application state. Concurrent orders within a case share that case's fixture inventory; they must not accidentally receive separate worlds. A designated restart reuses the same run identity and durable volumes. Only the named application container/process is terminated in application-crash cases; the fixture survives. Cancellation uses the application's cancellation boundary, not an automatic volume cleanup. Any fixture/collector failure outside the declared fault marks the run's affected evidence unavailable and preserves what exists; it is not converted into an agent success or an unrecorded rerun.

### Cold construction is a separate boundary

The accepted runtime model does not establish isolation of the builder's tools, connectors or inherited conversation. The builder must receive a curated, hashed export containing the candidate skill, declared sibling standards, authorized brief, public examples and supplied interface artifacts. Do not mount the authoring repository, its Git history, broad home folders or private fixture/evaluation stores. A fresh conversation without inherited history is necessary but does not confine filesystem or connector tools.

Before calling the build isolated, identify and test the actual builder runner, tool roots, connectors, environment, configuration discovery and input receipts. The current deliberation task is not that builder. If the selected runner retains ambient access to excluded sources, withhold the isolated-build claim and settle an enforceable runner binding; do not substitute an instruction to avoid reading them. This remains a campaign prerequisite, not a capability gap inferred from today's Docker probe.

## 4. PB-transport — public service and private control

PB-transport is accepted with plain HTTP/JSON for this local proving run. WIRE-route-method accepts one exact public business route, `POST /frd/v1/actions`, carrying the parent contract's action envelope. Here public means application-facing within the private container network, not internet-exposed. It is HTTP/JSON between independent processes, not an in-process call disguised as a tool. Exact schema bytes and runtime proof remain to be frozen. The protocol is specific to this proving fixture; the built agents' internal generation schemas remain builder-authored.

Use HTTP, not HTTPS, for the fixture action and observation/hook interfaces. Do not add fixture certificates, a trust bundle or certificate-lifecycle work to this run. Tad's rationale is to avoid certificate overhead in the synthetic, local proving environment. Plain HTTP provides neither transport encryption nor cryptographic service authentication; this is a deliberate limitation, not a claim that network separation provides TLS-equivalent protection. The scoped fixture credentials remain in headers and remain distinct from business approval, but are not encrypted in transit.

Keep this choice inside the accepted private, single-host container boundary with no host-published fixture/admin ports. Provider credentials are not fixture credentials; external model-provider connections retain end-to-end TLS through the transport-only relay. Access enforcement must still be verified before execution. Exposing the fixture beyond this local boundary or moving to non-synthetic business data reopens the transport decision; the proving-run choice is not a production or fleet-wide exemption.

- Send an opaque credential through the transport header, not model text or a claimed role field. The fixture maps it to a run/order/action scope. Credential values are neither fixture labels nor approval grants. Omit secrets from evidence; retain a non-secret credential identity for correlation.
- Authorization and schema checks precede mutation admission. Only a valid, identity-matching typed result can establish commitment or fenced non-application. An HTTP status alone cannot establish the fate of a logical operation, especially after another attempt.
- A valid action response uses the closed parent envelope. Malformed or missing responses remain distinguishable from typed business rejection. WIRE-route-method admits only the exact POST route before any body/authentication work, returning empty 404 for unknown paths and empty 405 plus `Allow: POST` for wrong methods on the exact path. Exact status/error unions and request limits remain the wire-schema pass; protocol, admission and business precedence are accepted in their recorded scopes, while runtime enforcement remains pending. No claim is made that the nine actions already have complete physical schemas.
- No automatic HTTP redirect, SDK retry, proxy replay or failover around fixture mutations. Each physical dispatch is recorded and budgeted; replay follows the accepted journal design. Disabling implicit retries must be verified in the actual client configuration and with a transmission counter.
- Implement reply-loss faults by actually withholding/closing the response at the declared boundary. Do not return a JSON object labelled timeout. Preserve service commitment and application observation as separate facts.
- Use a separate public observation/hook route with only the application's permitted projection and a non-coaching acknowledgement. It cannot list schedules, fetch evaluator results, release stock or request arbitrary admin actions. An application-supplied hook assertion is not independent proof of service commitment or actual provider bytes.

Private admin requests can initialize a fresh run, install the preregistered schedule, operate named hooks and export evidence. They cannot serve as an application recovery shortcut. The supervisor performs named crash/restart operations against the sealed identity. A trusted collector correlates service and application events while preserving their different origins; exact provider/parser instrumentation must still be bound without replacing the builder's adapter, parser or manifests.

## 5. PB-persistence — state, admission and evidence

### Accepted engine and pending enrollment

Use PostgreSQL for fixture persistence. The preceding SQLite proposal, its `BEGIN IMMEDIATE` mechanism and SQLite backup method are superseded, not alternative runtime paths to implement. PostgreSQL availability changed the setup-cost judgment; it did not change the accepted business or agent obligations. This is the fixture's storage choice, not a prescribed implementation for the cold builder's separate application checkpoints.

EN-scope accepts a dedicated HEB-154 database per run with restricted fixture credentials, holding inventory, orders, immutable effect records, grants, operation/attempt history, replay-permit state, event consumption and transaction evidence. Concurrent orders in a case share its inventory/database. A designated restart retains that run's database and accounting identity. Exact database/role names, object grants, initialization and retained-export custody remain bindings to complete; a dedicated name alone does not prove isolation. Do not reuse existing workbench tables, data or privileged credentials as product infrastructure.

The inspected local candidate is `sofia-wb-attempt2-postgres-1`, container `7a959de8d726d12046c95eb97b0048014972f2972d4a412dad3bc51cac533b14`, image `sha256:05794c28e6ee9515011cad7622a0c559780249101f324c73e0a4a45e31add205`, image reference `postgres@sha256:b260573038b3770e3d1aba5a9a033bdd19c4b16304b104d4b4256c75d8f15123`. Docker reported running/healthy, persistent data volume and restart policy `no`; an explicitly read-only metadata transaction confirmed PostgreSQL 17.11, effective TLS enabled, and `fsync`, `full_page_writes` and `synchronous_commit` enabled. These are observations of the selected surfaces at the preceding inspection, not a fresh probe, frozen campaign profile or durability test.

The complete mounted HBA file and its parsed rows declare TCP authentication using TLS plus SCRAM password and `clientcert=verify-full`; local peer access is for the postgres OS/database administrator. The server mounts workbench-owned configuration and a PKI volume. The HBA view describes current file contents, not necessarily the last rules loaded by the server; no fixture TCP authentication or denial test has run. [PostgreSQL HBA view](https://www.postgresql.org/docs/17/view-pg-hba-file-rules.html) [Client-certificate options](https://www.postgresql.org/docs/17/auth-pg-hba-conf.html)

Enrollment remains pending; the database credential condition is accepted. Preserve the existing server security and use a fixture-specific restricted login, password and matching client certificate with server-identity validation for the reuse path. Tad ratified this condition separately from the engine choice. It affects only the SQL connection; the accepted application-to-fixture interface remains HTTP and provider TLS remains unchanged. Do not bypass authentication through privileged Docker exec in the fixture, weaken shared HBA/TLS settings, or reuse workbench credentials. Exact credential issuance, custody and access bindings remain pending; this acceptance does not authorize provisioning. If the remaining enrollment checks prevent reuse, return a separate PostgreSQL instance for a hosting disposition rather than creating one implicitly.

Before enrollment, settle the exact connection path, database/role permissions and credential custody, and ensure the custodian can hold the shared image/configuration/lifecycle stable during a run. Concurrent workbench usage and connection/resource headroom have not been assessed. No server-wide crash, restart, global setting change, existing-data mutation or shared-volume cleanup is authorized by either the engine or credential-condition ratification. Proving-process crashes remain scoped to the named application/fixture components. Unexpected shared-server loss preserves affected evidence as unavailable; it is not silently repaired into a first-run success.

### Enrollment bindings — all five EN design directions accepted; exact enrollment pending

The five bindings below refine PB-persistence. Tad ratified EN-access's narrow shared authentication-file change, EN-route's dedicated internal database bridge and EN-scope's separate run database/owner/runtime authority as design only on 2026-09-11 (America/New_York), followed by EN-custody's dedicated credential issuance/custody and EN-lifecycle's serial run windows/bounded shared usage on 2026-09-12 (America/New_York). All five EN design directions are accepted. Exact enrollment bindings and proof remain pending; shared-server application, network attachment, provisioning and actual run-window activation remain unauthorized. If the remaining enrollment gates prevent reuse, return the hosting decision for a dedicated PostgreSQL instance, not weaker isolation or automatic provisioning.

Fresh read-only inspection confirmed the same container/image identity, existing workbench network, loopback-published port and TLS settings. The complete database ACL inventory shows `PUBLIC CONNECT` on `sofia_workbench` and the connectable maintenance/template databases. The complete non-secret role-attribute and membership inventories and grouped connection snapshot were also read. That snapshot showed only this inspection's client session; it is not workload headroom or a promise of future exclusivity. No business rows, password verifiers or private keys were read, and no new-role TCP test ran. PostgreSQL grants are additive: withholding a direct grant from the new fixture role does not remove its `PUBLIC` grant. This is why a dedicated database name and restricted login alone do not satisfy the intended connection boundary. [PostgreSQL privileges](https://www.postgresql.org/docs/17/ddl-priv.html)

Read depth for this proposal: physical binding, README, construct-spec instructions/template/evidence reference, testing skill/Haffey profile/strategy schema, recall project sheet, the matched recall episode, mounted-config source files and existing PKI initializer whole; latest ratification, parent/status pointers and Docker bridge/HBA documentation as scoped passages; PostgreSQL privileges/client SSL and Docker network-connect documentation whole. Truncated combined output was disclosed and affected passages reread separately. The recall Cloud SQL episode is a lifecycle caution, not evidence about Docker behavior. Commands, outputs, source identities and authoring-only checks are retained in the [enrollment proposal report](scenarios/proposed/pb-persistence-enrollment-authoring-checks.json).

#### EN-access — Restrict HEB-154 logins before the broad rules

Accepted design: a narrowly owned block in the existing mounted `pg_hba.conf`, before its broad TCP rules. The intended order is:

1. Preserve the existing local postgres/peer rule.
2. Allow only the exact active run database, fixture login and fixture source address, with the accepted TLS/client-certificate plus SCRAM authentication.
3. Reject every other TCP request by HEB-154 fixture logins, across IPv4/IPv6, encrypted/plaintext and database/physical-replication requests.
4. Preserve the existing workbench TCP rules in their original order and with unchanged authentication requirements.

Use a dedicated `NOLOGIN` marker role, `heb154_fixture_clients`, with no administrative attributes or explicit object grants; every HEB-154 fixture login belongs to it without membership-administration authority. Its direct/indirect membership selects the rejection rules. No workbench role joins it. Startup checks reconcile the complete HEB-154 login inventory against marker membership and exact allow rows, so a missing membership cannot silently escape the deny block. Create runtime roles as `NOLOGIN`; after rule/ACL checks, enable the exact role only for custodian-controlled TCP validation. Withhold the fixture's credential mount and campaign admission until those actual-role tests pass; disable the role and close only its identified sessions on validation failure. A different test identity cannot prove the runtime identity's access.

This shared-configuration amendment is accepted as design, not as permission to edit or reload the server. PostgreSQL uses the first matching HBA rule; a later reject cannot constrain an earlier broad allow. A reload is needed on this server after a separately approved file edit. Parsed file rows alone do not prove what new connections will experience. [PostgreSQL HBA matching and reload](https://www.postgresql.org/docs/17/auth-pg-hba-conf.html)

Do not revoke `PUBLIC` access on existing workbench databases, change existing roles, weaken TLS, alter the server certificate, or introduce a SQL proxy. Those are wider changes than this proposal. Bind the exact generated rule bytes and source addresses before freeze. Stage and test the rule block on a disposable synthetic PostgreSQL test instance first; shared-server application needs a saved preimage, verified mounted bytes, a controlled reload and a custodian-run workbench connection check. If that check fails, disable HEB-154 admission and restore the verified preimage only if no intervening edit exists; otherwise stop for reconciliation. Do not restart the shared server as recovery.

The [EN-access ratification](agent-code-v2-ratification-record.md#en-access--scoped-authentication-guard--ratified-2026-09-11-americanew_york) records the accepted scope and preceding draft identity. Its [authoring checks](scenarios/proposed/en-access-ratification-authoring-checks.json) refresh document integrity only; this ratification pass performed no new Docker, SQL or certificate inspection and no shared-server change.

#### EN-route — Dedicated internal database bridge

Accepted design: one HEB-154-owned internal bridge per active run, attached only to the fixture and the exact enrolled PostgreSQL container. The proposed name is `heb154-db-<run-token>`, where `<run-token>` is the run UUID rendered as 32 lowercase hexadecimal characters; retain the reversible mapping in the run manifest. Exact identifiers and address allocation remain freeze bindings. Never interpolate order/model text into network or SQL identifiers. Resolve collisions by refusing enrollment, not by adopting an existing object based on its name.

The custodian attaches the already-running database container as an external dependency; it is not a service that the proving project's Compose lifecycle may recreate or remove. Preserve its workbench attachment, published-port binding and default route. On the new network, use the network-scoped alias `postgres`, port `5432`, and a fixed fixture address allocated after checking the host/Docker route inventory for conflicts. The existing server certificate's public SANs were inspected and include `postgres`; a file inspection is not proof that a TCP peer serves that certificate. Require `sslmode=verify-full` with an explicitly supplied CA and certificate/key paths. [Docker network attachment](https://docs.docker.com/reference/cli/docker/network/connect/) [PostgreSQL client verification](https://www.postgresql.org/docs/17/libpq-ssl.html)

No desk, provider relay or evaluator joins this bridge; no fixture joins the workbench bridge. Do not expose a new host port, use host networking or route through `host.docker.internal`. Keep IPv6 disabled on the database bridge, while still checking alternative IPv6 paths from the application. Bind the fixture's HTTP listener to its business-network interface, not every interface: a second bridge must not publish the fixture service to the database side. Network membership is not directional or port-specific isolation. The permitted endpoints and denied alternate routes require actual probes. [Docker bridge boundaries](https://docs.docker.com/engine/network/drivers/bridge/)

The [EN-route ratification](agent-code-v2-ratification-record.md) accepts this design direction, not network creation/attachment or completed access verification. The [EN-route authoring checks](scenarios/proposed/en-route-ratification-authoring-checks.json) cover document integrity only. No new Docker, SQL or certificate inspection was performed for this ratification; the prior substrate observations retain their original limits.

#### EN-scope — Separate run database, owner and runtime authority

Accepted design: a fresh database per run, a separate `NOLOGIN` owner and a restricted runtime login. Proposed names remain `heb154_r_<run-token>`, `heb154_o_<run-token>` and `heb154_f_<run-token>` respectively; exact identifiers and grants are not frozen by this acceptance. Use a clean template and fixture-owned schema; do not copy workbench schemas, tables, extensions or data. The runtime login has no superuser, role/database creation, replication, RLS bypass, owner-role membership or server-file/program privileges. Its only membership is the powerless HEB-154 marker. The host custodian performs schema initialization/migration and scoped export as maintenance, outside application execution.

Remove `PUBLIC` privileges on the new run database and its user-defined schemas/functions before runtime admission; leave system catalogs and built-ins intact. Grant the fixture only connection, schema usage and the table/sequence/function operations explicitly needed by the frozen DDL. No runtime ownership, DDL, grant option or blanket future-object grants. Separate mutable projections from append-only history in the grant map; append-only tables must not receive update/delete/truncate merely because another fixture table needs them. Exact grants remain coupled to the pending DDL pass. A database created before its ACL tightening stays inaccessible to the fixture until admission is complete.

Concurrent orders in one case share this database; another run receives a different database/login. The designated restart retains the original database, login and run identity. The existing workbench roles must not acquire access to the new database through `PUBLIC`. Shared PostgreSQL catalogs can still reveal metadata such as role/database names, and host/database administrators remain trusted; this is not hostile-administrator, metadata-secrecy or resource isolation. The application still has no SQL route or credentials.

The [EN-scope ratification](agent-code-v2-ratification-record.md) accepts this storage/authority design, not exact object grants, provisioning or successful isolation. It does not prescribe the cold builder's separate application checkpoint technology. The [EN-scope authoring checks](scenarios/proposed/en-scope-ratification-authoring-checks.json) cover document integrity only; no database, role, grant or credential was created or changed in this ratification pass, and no new runtime inspection was performed.

#### EN-custody — New leaf credentials, existing CA kept in place

Accepted design: Tad is accountable custodian, with a host-only issuance operation that creates only the named HEB-154 client leaf under the existing database CA. Retain its key/password in a restricted per-run credential store outside the repository, build context, model text and evaluator evidence. Mount only that run's client certificate/key/password and public CA into the fixture, read-only, with effective file ownership verified for its non-root UID. Do not mount the shared PKI volume or export its CA/server/workbench private keys. Record non-secret credential identity and public certificate fingerprints, never secret values or password hashes.

The `db-pki-init.sh` inspected for the enrollment proposal was read whole: its fixed issuance set is the server and workbench client identities, and it can renew/reissue them. It is not an HEB-154 issuer. A separately scoped issuance operation must preserve existing CA/server/workbench leaf bytes and avoid shared serial-file races, missing-CA regeneration or incidental renewal. It requires explicit provisioning approval and an identified implementation before use; no new signer was built or run here. Do not solve an unavailable signer by copying the CA key or impersonating `wb_app`.

Accept a 30-day maximum client-leaf lifetime, valid through the declared run window, with no silent renewal during a run. Logical-case time cannot change certificate validity. The custodian disables the run login after evidence export or containment; doing so is not certificate revocation and does not close existing sessions. Close only identified HEB-154 sessions when required. Retain restart credentials until the run's restart/export obligations close; final credential/evidence retention and cleanup remain part of the pending custody policy. The exact private store path, issuance implementation and validity window must be frozen before enrollment.

The [EN-custody ratification](agent-code-v2-ratification-record.md) accepts this credential-custody design, not actual issuance, provisioning or verified secret isolation. The [EN-custody authoring checks](scenarios/proposed/en-custody-ratification-authoring-checks.json) cover document integrity only; no credentials were issued or mounted and no new Docker, SQL or certificate inspection was performed for this ratification.

#### EN-lifecycle — Serial run windows, bounded shared usage

Accepted design: one proving run at a time on this instance, retaining required concurrency inside that case. Reserve an operator-agreed window without workbench workload, migrations, PKI renewal, container recreation, server settings changes or host sleep. This does not authorize stopping workbench services. Tad must separately confirm the actual window; the connection snapshot cannot substitute for it.

Accept a fixture pool ceiling of four SQL connections with no overflow, plus at most two scoped custodian/export connections; leave the server's `max_connections=50` unchanged and do not consume its reserved emergency slots for the fixture. Check available ordinary connection slots against that six-connection envelope before admission. These are accepted ceilings, not measured sizing: CPU/memory/disk headroom, connection/statement/lock timeouts and bounded export duration remain preflight/freeze bindings. If they cannot fit without changing shared-server resources, return to the hosting decision. Do not weaken or serialize a case's required concurrency to fit a cap.

Record the actual container/image, network endpoints, database/role/grant map, mounted configurations, public certificate identities, selected effective durability settings and capacity observations at admission. Container recreation, unapproved relevant drift, unexpected shared-server loss or resource exhaustion stops admission and preserves affected evidence as unavailable. Do not silently reconnect to a replacement instance or repair the first result. Planned application crashes retain the database; shared-server crash/restart and host-power-loss tests remain out of scope.

For cleanup, first preserve and verify the scoped database export and isolated restore. Disabling a completed run's login or later removing its own database/role/network requires the exact run manifest and appropriate approval; no wildcard drops, shared-volume deletion or proving-project teardown of the database server. Retained data and credentials are not silently discarded. Remove only the owned HBA block/attachment at the later retirement gate after verifying that no active HEB-154 run or intervening configuration change depends on it.

The [EN-lifecycle ratification](agent-code-v2-ratification-record.md) accepts these operating boundaries, not an actual run window, provisioning, execution or cleanup. The [EN-lifecycle authoring checks](scenarios/proposed/en-lifecycle-ratification-authoring-checks.json) cover document integrity only; no new runtime inspection, capacity measurement, service stop, export/restore or cleanup was performed for this ratification.

#### Enrollment verification and next gate

These refinements remain part of FRDX-access, FRDX-evidence and FRDX-restart; they add no oracle cases or new acceptance verdict. Under Bedrock testing, require actual TCP authentication/authorization and process/network integration checks, not mocked connection results:

- The exact fixture identity reaches only its run database over verified TLS. Changing database, role, client certificate, password, source address, CA or hostname must produce the expected denial; a network error must not be mislabeled as an authentication-policy pass. Test plaintext and alternate IPv4/IPv6 paths independently. In a disposable synthetic instance, deliberately omit the HEB-154 reject block and confirm that the detector catches cross-database access through `PUBLIC`; never create that hole on the workbench server.
- The desk cannot reach SQL, database credentials, private admin or PKI paths, while its permitted fixture action works. A disposable intentionally connected canary must make the network-leak detector fire. Test the fixture's database-facing interface for unintended HTTP exposure. No real secret or business data is needed for any canary.
- Reconcile effective roles/memberships and object grants; permitted fixture operations succeed, forbidden DDL/owner elevation/history alteration and cross-run access fail. Use synthetic known-dirty grant/membership controls off the shared workbench instance. The custodian verifies the unchanged workbench connection path with its existing identity without disclosing that credential to this task or the fixture.
- A named application restart retains run state/identity; export and restore agree on a correlated snapshot. Inject a mismatched export and a changed enrollment identity into disposable checks to ensure detection. Preserve both original failure and cleanup failure evidence; do not clean up retained first-run evidence as ordinary test residue.

These are planned integration obligations gating enrollment, fixture acceptance and eventual HEB-154 landing, not executed tests or a schema-valid final test strategy. Numeric coverage and the broader campaign settings remain open. All five EN directions are accepted design; that does not authorize shared-server mutation, network attachment, provisioning or execution, or prove enrollment. Settle exact bytes, permissions, run-window and capacity bindings before any approved provisioning/verification step.

### Transactions and recovery — transaction and finality design accepted; exact bindings pending

The [PostgreSQL persistence-contract draft](postgresql-persistence-contract.md) records all four PG designs as accepted within scope. PG-finality includes pre-admission fencing and the reconciliation operation-kind/semantic-digest additions; PG-export includes the final seal, correlated snapshot and isolated restore. Exact relation/constraint and grant bindings, executable DDL, canonical identity encodings/wire bytes, numeric limits, export commands/versions, restore configuration, custody-policy physical bindings and enrollment remain open. Design acceptance does not authorize execution or prove these mechanisms.

Use the accepted short Read Committed write transactions with PG-transaction's fixed row-lock order and shared run guard; coherent multi-record reads use short Repeatable Read read-only transactions. Recheck current predicates inside each effect transaction, commit effect/result/history together, and keep hooks and other waits outside transactions and checked-out connections. Exact constraints, executable statements/grants and deadline enforcement still require binding before freeze. PostgreSQL's default Read Committed isolation does not make arbitrary multi-statement read/check/write sequences safe. No automatic effect-transaction retry is permitted in this fixture. Retain confirmed rollback as infrastructure-failure evidence; connection loss around commit remains uncertain until the permitted journal path resolves it. Neither condition silently dispatches another effect or grants fresh budget. [PostgreSQL transaction isolation](https://www.postgresql.org/docs/17/transaction-iso.html)

The proposed implementation sequence is:

1. Persist admission of the logical operation and physical attempt without a business effect. A replay atomically consumes its single-use permit with admission of its new attempt.
2. Reach the declared pre-commit hook outside an open write transaction. Never hold conflicting database locks while waiting for a barrier that needs another request to progress.
3. In the effect transaction, recheck the attempt's fence and current grant/references; enforce the cross-key active-allocation guard; write the reservation or arrangement together with its terminal journal result and service-event record. No transaction spans both business effects.
4. After commit, deliver or intentionally lose the response as scheduled. Recover pending event export from the committed record; do not rerun the mutation merely because the collector or client missed it.

Finality/replay closure serializes with admission and effect commitment for the same logical operation under the accepted PG-transaction/PG-finality design. Persistently fence every admitted prior attempt covered by the closure and the named original even if it was delayed before admission. Both late admission and late execution must honor that fence, with another check inside the effect transaction. A confirmed closure issues one generation-bound permit; only its single atomic consumption admits a fresh same-effect attempt in the next generation. Repeated delivery creates no second executor, and historical closure does not establish current finality after newer admission. Preserve append-only attempt events and current-state projections. Exact DDL/keys, comparison encodings, error schemas and export snapshot invariants remain to be frozen; no enforcement proof is claimed.

### PostgreSQL evidence export

Use PG-export's accepted sealed, correlated snapshot and isolated-restore design for only the HEB-154 run database. Initial capture precedes admission; final sealing occurs only after the declared terminal boundary or recorded harness abort/deadline, not at an application restart. Within the accepted two-connection custodian envelope, inspection and serial `pg_dump` share one exported snapshot; inventory every fixture relation, including empty ones. Restore into a fresh isolated instance with no workbench network or shared data/PKI mounts, then compare complete schema/data inventories, counts and row digests. Do not copy the live PostgreSQL data volume or export unrelated workbench databases. Exact compatible tool versions/command lines, deadlines, row encoding, restore-instance configuration and accepted custody-policy physical bindings remain open. This is design, not an executed backup or permission to create the restore instance. [PostgreSQL snapshot export](https://www.postgresql.org/docs/17/app-pgdump.html)

Pending attempts remain pending at the snapshot boundary. A sequence's maximum allocated value is not a commitment-order watermark: PostgreSQL sequence changes are visible across transactions and do not roll back with an aborted transaction. The frozen evidence protocol must identify actual visibility and correlated events without assuming that numbering proves finality. Export must not wait indefinitely or manufacture finality. [PostgreSQL sequence caveat](https://www.postgresql.org/docs/17/transaction-iso.html)

The initial and final snapshots, full transaction history and actual delivered observations remain separate artifacts. Sealing stops further fixture mutations; it does not close pending attempts, release stock or establish what the application knew. Snapshot custody cannot upgrade hidden service truth into application knowledge. Raw synthetic request/output retention is governed by the accepted content and custody policies, whose physical implementation and controls remain pending. Preserve original evidence, unavailable seals, incomplete exports and restore mismatches; verify the copy's integrity and complete logical contents before using it. No silent substitute, automatic unseal or evidence cleanup follows. Source runtime grants/ownership and external producer-completion evidence remain separately retained; data restoration does not prove either. Retention/cleanup requires the accepted exact-target operator gate, not destructive Compose teardown.

## 6. Planned verification, not results

These proposed checks refine existing FRDX obligations; they do not create a new oracle population. Bedrock testing supplies the isolation, actual-boundary and counterfactual discipline. The selected realization remains the Haffey pytest profile. They are obligation-level design, not executable tests or a schema-valid final test strategy: the deterministic thresholds are accepted at 100% registered fixture and dirty-control passage, 90% line and branch coverage, 80% mutation score and 100% critical branch/state exercise. Evaluation sampling is accepted as one complete public and three hidden first-attempt draws per case under each of three provider-configuration classes, with public/hidden verdict separation and retained infrastructure-rerun lineage. Evaluation thresholds are accepted as two-sided 95% Wilson lower floors of 80% first-attempt and 90% recovered, a two-of-three per-case rule, zero invariant violations and 100% evaluator-control passage. Hidden-population construction is accepted as exactly 37 confidential, materially varied counterparts with an evaluator-only eligibility map, independent custody/review and identity-changing amendments. VERIFY-evaluator-contract is accepted as deterministic-oracle-first, independently configured residual semantic judgment with versioned rubrics/calibration, frozen judge identity, untrusted-output handling and named human adjudication. VERIFY-provider-matrix fixes OpenAI-only A, Anthropic-only B and OpenAI/Anthropic/OpenAI mixed provider-family assignments; Gemini stays in its separate live-profile gate. VERIFY-model-settings fixes the candidate product-role models and request settings. Actual implementation paths, hidden content/assignments, physical custody, evaluator artifacts/settings, physical candidate profiles, frozen pricing and spend authorization still need explicit disposition. No invented passing result is supplied.

VERIFY-pricing-and-spend-gate fixes a fresh content-addressed price input and one exact operator authorization with a USD $160 candidate-role ceiling. Its 24-hour launch freshness, complete charge dimensions, campaign/profile/principal/expiration bindings and fail-closed drift/unknown handling are test obligations. The evaluator and Gemini remain outside this authorization, and no physical pricing record, spend record or provider call exists yet.

| Existing obligation | Actual boundary to exercise | Clean/known-dirty discrimination required |
|---|---|---|
| FRDX-access | Container filesystem, effective mounts, credential scopes, public/admin routes and egress | Permitted own-order read succeeds. Private canary read and wrong-order/admin access fail. A deliberately misconfigured disposable canary setup must make the leak detector fire; do not weaken the campaign environment. Include inherited builder-context/configuration channels in the separate build-access check. |
| FRDX-approval | Actual HTTP client and service admission | Exact approved dispatch succeeds; forged principal/grant, changed plan and early dispatch are rejected and remain visible even when the service prevented an effect. |
| FRDX-reservation | Real concurrent service requests and persistent inventory | Both mirrored winners work; a second key for the same active allocation, over-allocation and fabricated receipt are detected from state. Barrier placement must not hold locks needed by another request. |
| FRDX-arrangement | Database transaction and independent post-crash read | Matching reservation/order update and journal commit together. Terminate at the pre/post-commit boundaries; a deliberately separated state/journal commit must be caught. |
| FRDX-finality | Actual service admission, delayed attempt and permit race | Release a delayed original after closure and consume a permit twice. Observe one permitted effect at most, immutable history and no premature controller replay. A mocked reconciliation response alone is insufficient. |
| FRDX-restart / FRDX-cancel | Real application process death or cancellation, persistent volumes and actual dispatch counts | Preserve effects, original identity and consumed budget. Detect world/budget reset, repeated reservation, post-cancel work and silent compensation. |
| FRDX-exposure | Actual prerequisites and one-shot hooks | Stop before a required seam and retain unexposed status plus original failure. Detect duplicate firing or a harness-supplied substitute answer. |
| FRDX-evidence | Collector failure/export, consistent snapshot and original/delivered records | Detect an omitted first failure, event-origin spoof, a mismatched snapshot cut, or injection counted as natural model output. Independent state inspection must detect a misleading receipt. |

Fixture integration tests may use explicitly labelled deterministic clients/doubles to challenge these service boundaries. They do not count as cold construction or resulting-agent accuracy. Product campaign tests still require the actual cold-built application and declared live provider configurations. Concurrent tests receive distinct databases/volumes except where a case deliberately shares stock. Pin test configuration, collection roots, imports and plugins; verify the effective scope and disable telemetry export before application import under the selected profile.

These obligations gate fixture acceptance and eventual HEB-154 landing. Process-crash, concurrency and access tests are required by the stated risks. Host power-loss, hostile-host escape, distributed failover, production load and deployment/migration proof are outside this fixture; changing those claims reopens the relevant design. Property/fuzz expansion and exact coverage, sampling and resource budgets remain freeze work. A missing environment or unavailable observation remains unavailable evidence.

## 7. Remaining work and lifecycle

| Work surface | State after this draft |
|---|---|
| Four EC behavior decisions | Accepted; not reopened |
| PB-runtime / PB-transport / PB-persistence | Runtime model, local HTTP, PostgreSQL engine and database credential condition accepted within recorded scopes |
| Existing PostgreSQL instance enrollment | Database credential condition and all five EN designs accepted in §5; exact bindings, actual run window, capacity and proof pending; no provisioning, network attachment, shared-server application or execution authorized |
| Exact public/admin/hook/evidence schemas and bytes | Pending; include sizes, limits, identities and version compatibility; listener route/method plus specified protocol/admission/business precedence is accepted, with runtime proof pending |
| Operator policy and canonical grant/plan encoding | Pending; cannot consult evaluator labels or coach a replacement plan |
| PostgreSQL DDL, isolation/locking, connection policy, finality and export implementation details | All four PG designs accepted within scope; exact wire bytes/DDL/grants, numeric limits, export/restore and custody-policy physical bindings open; runtime proof pending |
| Launch manifest, immutable image/dependency identities, actual mounts and relay policy | Pending; no launchable configuration authored |
| Actual cold-builder runner and hidden custody | Pending; runtime containers alone do not close this gate |
| Test-strategy schema, calibration, provider/content/budget/sampling/accuracy settings | Pending explicit campaign dispositions |
| Physical freeze, fixture/candidate implementation, cold build and verification | Not performed; prior gates remain in force |

Recommendation: finish the schema pack's non-JSON member artifacts and final frozen inventory, instantiate the accepted verification-contract shape with its accepted deterministic and evaluation sampling/threshold rules, and complete remaining projection fields and physical bindings before deriving corresponding complete schemas, DDL/grants and export encodings. The [wire/identity companion](wire-and-identity-contract.md) records all four WIRE designs accepted within scope, including pending-mutation unresolved and original committed-result bytes. The public approval policy/opportunity templates, all nested root bindings, fixed contract-set/build-input/application-output/fixture/execution/evidence-policy membership, test-schema-pack mechanics and 42-schema v2 seed, verification-contract shape, deterministic thresholds, evaluation sampling and evaluation thresholds, fixture configuration/event/SQL-deadline/mechanism-artifact policies, capability requirements, aggregate budget, control policy and all three inner evidence policies, dual manifest identity, private pre-execution boundary, deterministic audience projections and exact root are authored; exact whole-run instantiation and runtime proof remain pending. A [machine-schema candidate](schemas/proposed/README.md) is now authored with advisory tests. The complete public initial and scheduled-event source populations have exact per-case selection; actual build/application/fixture/provider/execution/evidence-policy/mechanism artifacts, hidden-population and eligible-denominator materialization, complete campaign-ledger and final contract-set generation, authenticated runtime loading/publication and omission proof remain pending. Listener, protocol, admission, business and approval-issuance precedence are accepted in their recorded scopes; remaining physical bindings and freeze remain pending. All four PG designs are accepted within scope, but exact physical bindings remain open. All five EN directions are accepted design only; they do not enroll the instance. Keep the cold builder's agent/controller implementation out of supplied fixture artifacts. The schema-pack and verification-contract decisions are limited to testing the `agent-code` skill and make no Agents of HEX overall-design choice. Complete remaining physical schemas/policies, launch/enrollment and operational bindings; settle remaining campaign settings; freeze the relevant artifacts; only then implement and prove under the accepted sequence. Present substantive new choices for ratification. The retrospective remains deferred until the end.

## Change log

| Revision | Date | Change |
|---|---|---|
| draft-1 | 2026-09-12 | Runtime/local-HTTP choices, PostgreSQL engine, credential condition, all five EN, all four PG and all four WIRE designs ratified within scope; exact schemas/physical bindings/enrollment open; no provisioning, shared-server application, network attachment, implementation or execution authorized |
