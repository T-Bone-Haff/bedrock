# HEB-133 proving profile

## Binding and representative consumer

The change binds authors of execution relays and design-review charges and authors proving reusable standards. The representative consumer is the reconstructed RBT-114 Gate I branch-name boundary, grounded in the complete retained corrections-record section but explicitly not a byte-for-byte package replay.

## Deterministic cases

- Consumer-derived demand rejects a nonconforming reconstructed head and accepts the recorded corrected head.
- A complete instrument demand accepts; a summarized outcome taxonomy rejects.
- Touch-set derivation accepts complete edit-locus coverage and rejects an omitted locus.
- Permission evidence alone cannot establish reachability; custody plus code-path and actor-flow checks can establish structural exclusion.
- A contrary-input control confirms a premise only when the detector observes the contrary result.
- An empty result is unproved until a counterfactual demonstrates detector effectiveness.
- Validator mutation tests remove required skill, template, and reviewer-charge carriers and must fail closed.

## Judgment checks

- Rules remain task-local to relay construction, review-charge construction, and standard proof.
- HEB-131 runtime population semantics and HEB-137 recall metadata are not mirrored.
- Verdict-bearing pinning is narrow; incidental context is not promoted into a universal requirement.
- The installed skills do not editorialize about the breadth of the originating evidence.

## Thresholds

Adoption requires every deterministic fixture and seeded mutation test to pass, the reconstructed consumer to reject/accept in the declared directions, all changed Markdown carriers to receive rule-trigger disposition review, the full repository suite and package validators to pass, and routing policy to remain satisfied. Any unavailable required check, unresolved trigger disposition, or false replay claim blocks adoption.

## Reproduction

Run:

```text
python -m unittest tests.test_authoring_contracts -v
python scripts/validate_authoring_contracts.py
python scripts/validate_rule_trigger_audit.py --require-complete
python -m unittest discover -s tests -v
python scripts/validate_plugin.py --inventory docs/evidence/heb-109/metadata-inventory.json
bash scripts/smoke_clean_install.sh
```
