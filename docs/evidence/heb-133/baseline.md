# HEB-133 baseline

Captured 2026-08-26 ET after fetching `origin`. Local `main`, `origin/main`, and the HEB-133 feature-branch base matched at `74e146bb94cf73ea126cdf92659f451f783b08d8`; the worktree was clean and the manifest candidate was `8.2.0`.

The operator ratified the relay/review-charge scope and placement, the decision to leave `relay.schema.json` at v1, and the narrow HEB-131 boundary. HEB-131 retains general runtime population, staleness, and mismatch semantics; HEB-133 owns only touch-set derivation from ratified edit loci. HEB-137 owns the deferred recall-vault graduation-metadata slice. HEB-131 and then HEB-128 are the next bodies of work after HEB-133 lands.

## Source depth and limit

The HEB-133 ticket, ratified-scope comment, target skills and templates, authoring-contract validator and fixtures, HEB-136 live audit, and relevant RBT-114 corrections-record sections were read whole. Unbounded filename and narrowed Markdown/text content searches across the supplied Working and Development Archives roots did not find the named session handoff or original Gate I package revisions.

The corrections record preserves that Gate I rev1 demanded a head name rejected by the consuming `gates` job and that rev2 corrected it to `feature/rbt-114-v1.1-chain`. The retained fixture may reconstruct that behavioral boundary; it must not claim the unavailable rev1 package bytes or rejected spelling.

## Candidate classification

This amendment adds reusable behavior without removing or changing an existing accepted interchange field. `relay.schema.json` remains v1; consumers that conform today remain valid. The candidate therefore advances one minor version from `8.2.0` to `8.3.0`. This is candidate identity, not release proof.
